#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b, c, d;
    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;
    if (pow(a, b) > pow(c, d))
        cout << "YES";
    else
        "NO";
    return 0;
}